name: Weather Type Prediction

conda_env: conda.yaml

entry_points:
    main:
      parameters:
        dataset: {type: string, default: "weather_dataset_preprocessing.csv"}
      command: "python modelling.py {dataset}"